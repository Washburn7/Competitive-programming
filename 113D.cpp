#include <bits/stdc++.h>

using namespace std;
using ll = long long;

constexpr int N = 2e5 + 10;

void solve() {
    int n, m;
    cin >> n >> m;
    int a, b;
    cin >> a >> b;
    vector<double> p(n + 1);
    
    for(int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    t = 1;

    while(t--) {
        solve();
    }

    return 0;
}